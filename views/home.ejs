<%- include("partials/header") %> 
<h1>Home</h1>
<p>
    <%= homeStartingContent %> 
</p>
<% posts.forEach(post => { %>
    <div>
        <h1>
            <%= post.title %> 
        </h1>
        <p>
           <%= post.body %>  
        </p>
    </div>
<% }); %> 

<%- include("partials/footer") %> 